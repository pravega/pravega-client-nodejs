codecov:
  require_ci_to_pass: true
  max_report_age: off
  notify:
    wait_for_ci: true

coverage:
  status:
    project:
      default:
        threshold: 0.5%
    patch:
      default:
        target: 80%